# .github/workflows/test.yml
name: Test

on:
  push:
    branches: [ main, develop, opensource/ci-evaluation ]  # ← 只有推送到这些分支才触发
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello"
