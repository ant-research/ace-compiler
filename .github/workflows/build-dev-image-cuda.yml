# ============================================================
# ACE-Compiler Dev Docker Image Build (CUDA)
#
# Builds and pushes CUDA dev Docker images to Docker Hub.
#
# Usage:
#   - Manual dispatch: build and push CUDA image
#   - Scheduled: rebuild images weekly
# ============================================================

name: Build Dev Image (CUDA)

on:
  push:
    branches:
      - master
      - 'release/*'
      - 'opensource/ci-evaluation'
  workflow_dispatch:
    inputs:
      cuda_version:
        description: 'CUDA version'
        required: false
        default: 'cu124'
        type: choice
        options:
          - 'cu124'
  schedule:
    - cron: '0 0 * * 0'  # Weekly on Sunday

env:
  DOCKERHUB_USER: ${{ secrets.DOCKERHUB_USER }}
  DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

jobs:
  build-cuda-image:
    name: Build CUDA Image (CUDA ${{ matrix.cu_version }})
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - { cu_version: "cu124", torch: "2.5.0", torchvision: "0.20.0", torchaudio: "2.5.0", torch_index: "https://download.pytorch.org/whl/cu124", base: "nvidia/cuda:12.4.0-devel-ubuntu22.04" }

    steps:
      - uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USER }}
          password: ${{ env.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.dev.cuda
          push: true
          tags: opencc/dev:${{ matrix.cu_version }}
          build-args: |
            BASE_IMAGE=${{ matrix.base }}
            PIP_INDEX_URL=https://pypi.org/simple
            TORCH_VERSION=${{ matrix.torch }}
            TORCHVISION_VERSION=${{ matrix.torchvision }}
            TORCHAUDIO_VERSION=${{ matrix.torchaudio }}
            CU_VERSION=${{ matrix.cu_version }}
            TORCH_INDEX_URL=${{ matrix.torch_index }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
